import { ListAccessibilityDirective } from './list-accessibility.directive';

describe('ListAccessibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ListAccessibilityDirective();
    expect(directive).toBeTruthy();
  });
});
